{{#if disabled}}
  <div class="lattice-admin-disabled">{{i18n 'lattice.admin.disabled'}}</div>
{{else}}
  <div class='row groups'>
    <div class='content-list span6'>
      <h3>{{i18n 'lattice.admin.page_title' name=siteSettings.lattice_name}}</h3>
      {{#if model}}
        <ul>
          {{#each lattice in model}}
            <li>
              {{#link-to 'adminLattice' lattice.id}}{{lattice.title}}{{/link-to}}
            </li>
          {{/each}}
        </ul>
      {{else}}
        <p>{{i18n 'search.no_results'}}</p>
      {{/if}}
      <div class="controls">
        {{#link-to 'adminLattice' 'new' class="btn"}}
          {{fa-icon "plus"}} {{i18n 'lattice.admin.new' name=siteSettings.lattice_name}}
        {{/link-to}}
      </div>
    </div>
    <div class='content-editor'>
      {{outlet}}
    </div>
  </div>
{{/if}}
